CREATE TABLE IF NOT EXISTS ACCESS_RECORD (
    OBJECT_ID   VARCHAR,
    ELAPSE_MS   INT,
    TIMESTAMP   TIMESTAMP,
    VIA         VARCHAR,
    HOST        VARCHAR,
    THREAD_ID   VARCHAR,
    USER_AGENT  VARCHAR,
    QUERY       VARCHAR,
    SESSION_ID  VARCHAR,
    FORWARDED_FOR   VARCHAR,
    REQUEST_URL VARCHAR,
    USER_ID     VARCHAR,
    ORIGIN      VARCHAR,
    DATE        DATE,
    METHOD      VARCHAR,
    VM_ID       VARCHAR,
    STACK       INT,
    INSTANCE    VARCHAR,
    SUCCESS     BOOLEAN,
    RESPONSES_STATUS    INT
)
