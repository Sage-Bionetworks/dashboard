package org.sagebionetworks.dashboard.model;

public enum Aggregation {

    /** Aggregates every 3 minutes. */
    m3,

    /** Aggregates hourly. */
    hour,

    /** Aggregates daily. */
    day
}
