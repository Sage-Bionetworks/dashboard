package org.sagebionetworks.dashboard.model.redis;

public enum NameSpace {

    /** The set of names. */
    name,

    /** The set of IDs. */
    id,

    /** Time series metrics. */
    timeseries,

    /** Counts */
    count
}
